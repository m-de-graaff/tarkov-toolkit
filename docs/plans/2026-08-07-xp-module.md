# XP Module Plan

> **To execute:** use the `executing-plans` skill. Steps use `- [ ]` for tracking.

**Goal:** An /xp page that answers "what is the best way to level each skill". Two content kinds: a computed Crafting optimizer (cheap continuously-running crafts, since craft skill XP scales with production time) and a curated method guide for the practical skills (Metabolism, Endurance, Strength, Vitality, Immunity, Surgery, Covert Movement, Attention/Search, Intellect, Charisma, Hideout Management).

### Task 1: shared prices hook
- [x] Extract `lib/usePrices.ts` from MarketPage (cache-first, auto-fetch when stale, 30 min interval, per game mode; auto-fetch skipped under MODE=test so pages stay testable). Refactor MarketPage onto it. Suite green, commit.

### Task 2: crafting optimizer lib
- [x] `lib/craftingXp.ts`: `craftingLevelingRows(crafts, prices, hideoutLevels, onlyBuilt)` -> rows {craft, materialCost, costPerHour} sorted by costPerHour ascending; tools excluded from cost; unpriceable crafts dropped; onlyBuilt filters to stations at or above the craft level. Export `craftMaterialCost` from profit.ts internals. Unit tests with fixture prices.

### Task 3: guide data + page
- [x] `data/skillGuides.ts`: 11 curated skills, each {name, gives, methods[{title, detail, cost: free|cheap|expensive, pace: passive|steady|fast}]}. Factual, terse, no fluff.
- [x] `pages/XpPage.tsx`: intro, search, skill cards with method lists and cost/pace badges; the Crafting card embeds the computed table (top 15, "only my stations" toggle, prices status + load control). Route /xp, TopNav tab, Home card. Page smoke test (static content) + toggle test.
- [x] Suite + build green, commit; merge to main and push (pre-authorized).

{
  "name": "raidplanner",
  "private": true,
  "packageManager": "pnpm@11.11.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "snapshot": "pnpm --filter @raidplanner/data snapshot",
    "watcher": "pnpm --filter @raidplanner/watcher start"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
